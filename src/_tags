# Compilation options
<util/*>: for-pack(Tabsat)
<base/*>: for-pack(Tabsat), package(ocamlgraph)
<parsers/*>: for-pack(Tabsat)
<input/*>: for-pack(Tabsat), package(cmdliner)
<core/*>: for-pack(Tabsat), package(msat), package(cmdliner)
<main.*>: package(msat), package(cmdliner), package(ocamlgraph)

# enable stronger inlining everywhere
<util/vec.cmx>: inline(15)

# more warnings
<**/*.ml>: warn_K, warn_Y, warn_X
<**/*.cm*>: debug

