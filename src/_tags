# Compilation options
true: color(always)

<util>: include
<base>: include
<misc>: include
<core>: include
<test>: include
<algos>: include
<input>: include
<middle>: include
<output>: include

# Some warnings
<**/*.ml>: warn_K, warn_Y, warn_X, warn(-58)
<**/*.cm*>: debug

# Package management
<*/*.*>: for-pack(Archsat)

<**/*>: package(unix), package(zarith), package(msat),     \
        package(cmdliner), package(ocamlgraph),            \
        package(containers), package(containers.data),     \
        package(gen), package(oclock),                     \
        package(dolmen), package(qcheck),                  \
        package(sequence), package(spelll)

