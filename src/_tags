# Compilation options
<**/*>: for-pack(Tabsat), package(containers)

<base/*>: package(ocamlgraph)
<core/*>: package(msat), package(cmdliner)
<input/*>: package(cmdliner)

<main.*>: package(msat), package(cmdliner), package(ocamlgraph), \
          package(containers), package(containers.data)

# enable stronger inlining everywhere
<util/vec.cmx>: inline(15)

# more warnings
<**/*.ml>: warn_K, warn_Y, warn_X
<**/*.cm*>: debug

